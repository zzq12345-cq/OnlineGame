#include "iostream"
using namespace std;
/*
 * 数列：1、8、43、4、23、11、58、79、77、…。第一项是 1，之后每一项都是前
一项的值乘以 5 加 3。输入一个正整数 n，输出数列第 n 项对 107 取余数的结果。
说明：数学上，(𝑥 × 𝑦)%𝑧 = (𝑥 × (𝑦%𝑧))%𝑧 = ((𝑥%𝑧) × (𝑦%𝑧))%𝑧
 */

int main()
{
    int n, a = 1;
    cin >> n;
    for (int i = 0; i < n - 1; ++i) {
        a =  (a * 5 + 3) % 107;
    }
    cout << a ;
    return 0;
}